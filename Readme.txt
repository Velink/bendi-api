Instructions to Run 

1 - Open Code Editor (for example Visual Studio Code)
2 - Enable Live Server Extension and open a localhost 
3 - Open the terminal and enter the command 'json-server --watch db.json'
4 - Choose the column you want to sort the table through, by entering a 
query parameter. 

For example:
'localhost:5503/?orderByField=city'
